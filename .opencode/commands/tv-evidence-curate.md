---
description: Validate exact TV screenshot evidence and emit a compact manifest for final design review.
agent: tv-evidence-curator
subtask: true
---

Use `$ARGUMENTS` as the complete evidence contract. It must name every exact
image and metadata path, expected state matrix, surface, build or commit,
dimensions, density, font scale, locale and focus state. Check coverage,
consistency, duplication, staleness and private data. Do not discover evidence
or make design judgments.
