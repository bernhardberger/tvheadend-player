interface:
  display_name: "Gradle Run"
  short_description: "Run standard Gradle with bounded execution"
  default_prompt: "Use $gradle-run for bounded execution and private diagnostics without a workflow ledger."
