<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.TVHeadendPlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Status/navigation bar -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>

        <!-- optional -->
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>

    <style name="Theme.TVHeadendPlayer.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenBackground">@color/splash_screen_background</item>
        <item name="postSplashScreenTheme">@style/Theme.TVHeadendPlayer</item>
    </style>

</resources>
