# Legacy screenshots

The images in this directory have no recorded source revision, build identity,
device manifest, or validation date. They are historical, non-authoritative
evidence and must not be used to infer the current UI.

Use an image only when an assignment names its exact path for an explicitly
historical comparison. New device evidence belongs in the ignored, revision-
scoped `captures/device/` workflow rather than this directory.
